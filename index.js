// run `node index.js` in the terminal

const checkWordsInCrossword  = (inputMatrix, m) => {
    // Work on this function here.
    return [];
};


function main_caller(){
console.log(`Hello Node.js v${process.versions.node}!`);

const input = ["COMATFBS","ROGAAOAA","EGMLSALM","EGOPHSLP","DYWHUMEL","DJEAYTRE","OKUAZDER","METADATR"];
const m = ["COMPUTER","ALPHA","META","METADATA"];

const res = checkWordsInCrossword(input, m);
console.log("Final Output",res);
};

main_caller();